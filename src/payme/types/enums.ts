export enum TransactionState {
  CREATED = 1,
  COMPLETED = 2,
  CANCELLED = -1,
  CANCELLED_AFTER_COMPLETE = -2,
}

export enum OrderState {
  WAITING_PAY = 0,
  IN_PROGRESS = 1,
  ACCEPTED = 2,
  CANCELLED = 3,
}

export enum PaymeStatus {
  INVALID_AMOUNT = -31001,
  TRANSACTION_NOT_FOUND = -31003,
  NO_REFUND = -31007,
  COULD_NOT_PERFORM_TRANSACTION = -31008,
  ORDER_NOT_FOUND = -31050,
  ORDER_IN_PROGRESS = -31051,
  ORDER_ACCEPTED = -31052,
  ORDER_CANCELLED = -31053,
  TRANSACTION_ALREADY_CREATED = -31054,
  SYSTEM_ERROR = -32400,
  INVALID_ACCESS_DATA = -32504,
}
